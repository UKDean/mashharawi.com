/* The same tokens as assets/site.css, so a rendered post and the site are
   visibly the same object. If a colour changes on the site, change it here
   too: this file is the only place the video knows about them. */
export const T = {
  mill: '#E4E6E3',   // galvanized sheet, the ground
  sheet: '#F3F4F1',  // raised surface
  ink: '#101418',    // graphite
  scale: '#68727A',  // mill scale grey
  line: '#C9CDC8',   // hairline
  blued: '#1F3B4D',  // blued steel
  hot: '#D8600F',    // hot-rolled amber
};

export const FPS = 30;
export const SIZE = 1080; // LinkedIn's square slot, the one that crops least
